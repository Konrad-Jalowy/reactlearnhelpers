import { useRef } from "react";
function getKey(key){
    return `single-${key}`;
}
function AddOneRFC({setWords}){
    const keyRef = useRef(1);
    return (
        <>
        <p>Not implemented</p>
        </>
    );
};

export {AddOneRFC};